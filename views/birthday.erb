<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Birthday Greeter</title>
  </head>
  <body>
    <p>Your birthday is <%= @birthday.date.strftime(@date_format) %></p>
    <p>Today's date is <%= @today.strftime(@date_format) %></p>
    <p>You are <%= @diff %> days old 😬</p>
    <% if @this_year_diff > 0 %>
    <p>Your birthday was <%= "#{@this_year_diff} #{@day_days}" %> ago</p>
    <% elsif @this_year_diff < 0 %>
    <p>Your birthday is in <%= "#{@this_year_diff.abs} #{@day_days}" %></p>
    <% else %>
    <p>It's your birthday today! 🎂</p>
    <% end %>
    <p><a href="<%= @wiki_link %>">Click here for info from Wikipedia on your birthday!</a></p>
  </body>
</html>
